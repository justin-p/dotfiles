# Resolve .zsh_alias.d next to this file (works when ~/.zsh_alias is a stow symlink).
_alias_dir=${${(%):-%N}:A:h}/.zsh_alias.d
for _alias in ${_alias_dir}/*.zsh(N); do
  source "$_alias"
done
unset _alias _alias_dir
